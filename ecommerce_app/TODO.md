# TODO for Fixing cart_provider.dart

- [ ] Fix syntax errors: Add spaces in method declarations (e.g., `Future<void> placeOrder()`)
- [ ] Update `CartItem.fromJson` to handle missing 'quantity' by defaulting to 1
- [ ] Modify `removeItem` to decrement quantity if >1, else remove the item
